local a = {}
local b = game:GetService("HttpService")
if readfile("VHS/LineColor.txt") then for i,v in b:JSONDecode(readfile("VHS/LineColor.txt")) do a[i] = Color3.fromRGB(v.R, v.G, v.B) end end
return a
